// Here we're creating the comments reducers
// For now, it will only have the default state defined

export default function(state = [], action) {
  switch (action.type) {
    default:
      return state;
  }
}
