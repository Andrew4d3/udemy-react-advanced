import React from "react";
import ReactDOM from "react-dom";

import App from "./components/App";

// Here we are linking the main App component with the index HTML file
ReactDOM.render(<App />, document.querySelector("#root"));
